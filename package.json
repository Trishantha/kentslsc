{
  "name": "kentslsc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "command -v pnpm >/dev/null 2>&1 || { corepack enable && corepack prepare pnpm@11.21.0 --activate; } && pnpm --filter @kentslsc/database build && pnpm --filter @kentslsc/shared build && pnpm --filter @kentslsc/api build && pnpm --filter @kentslsc/web build",
    "deploy:hostinger:frontend": "command -v pnpm >/dev/null 2>&1 || { corepack enable && corepack prepare pnpm@11.21.0 --activate; } && pnpm install --frozen-lockfile && pnpm --filter @kentslsc/database build && pnpm --filter @kentslsc/shared build && pnpm --filter @kentslsc/web build",
    "deploy:hostinger:backend": "command -v pnpm >/dev/null 2>&1 || { corepack enable && corepack prepare pnpm@11.21.0 --activate; } && pnpm install --frozen-lockfile && pnpm --filter @kentslsc/database build && pnpm --filter @kentslsc/api build",
    "build:clean": "pnpm clean && pnpm build",
    "start": "node server.js",
      "dev": "node server.js",
    "dev:clean": "pnpm clean && pnpm dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck",
    "clean": "rm -rf .turbo apps/*/.turbo packages/*/.turbo apps/web/.next apps/api/dist packages/*/dist",
    "db:generate": "pnpm --filter database generate",
    "db:migrate": "pnpm --filter database migrate",
    "db:seed": "pnpm --filter database seed",
    "db:studio": "pnpm --filter database studio"
  },
  "devDependencies": {
    "turbo": "^2.10.9"
  },
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20"
  }
}
